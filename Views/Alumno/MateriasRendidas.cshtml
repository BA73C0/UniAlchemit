@{
    ViewBag.Title = "Materias Rendidas";
}

@model List<Pruebas2.Models.ReporteViewModel>


<body style="background-color: #1BB769; font-family: Arial">
    <br />
    <div class="submit-btn3">
        <h1>Materias Rendidas</h1>
    </div>
    <br />
    <div class="caja4">
        <table class="table table-striped">
            <tr>
                <th>ID</th>
                <th>Materia</th>
                <th>Fecha</th>
                <th>Nota</th>
                <th>Estado</th>
            </tr>
            @if (Model[0].Materia != "Ejemplo")
            {
                foreach (var inscripcion in Model)
                {
                <tr>
                    <td>@inscripcion.IDMateria</td>
                    <td>@inscripcion.Materia</td>
                    <td>@inscripcion.Fecha</td>
                    <td>@inscripcion.Nota</td>
                    @if (inscripcion.Nota >= 4)
                    {
                        <td>Aprobado</td>
                    }
                    else if (inscripcion.Nota < 4 && inscripcion.Nota > 0)
                    {
                        <td>Desaprobado</td>
                    }
                    else if (inscripcion.Nota == 0)
                    {
                        <td>Cursando</td>
                    }
                    else if (inscripcion.Nota == -1)
                    {
                        <td>Ausente</td>
                    }
                </tr>
                }
            }
        </table>
    </div>
    <br />
    <button class="submit-btn2" onclick="window.location.href='@string.Format("https://localhost:44348/Alumno/MateriasAlu/{0}", Model[0].IdAlumno)'">Volver</button>
</body>

